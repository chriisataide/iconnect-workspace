"""Serviços do Workspace.

Vazio no ST-002. Entram nas ondas seguintes (Etapa 5 §5.2):

    aprovacao.py    decidir(), decidir_em_lote()
    solicitacao.py  solicitar() — cria o ticket no módulo certo
    publicacao.py   publicar() — ciclo de vida de documento
    indexacao.py    indexar_documento() — consome search_documents() dos providers
    widgets.py      runtime: permissão → cache por geração → provider com timeout
"""
